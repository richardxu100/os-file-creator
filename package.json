{
  "name": "eip-rename",
  "version": "0.0.1",
  "description": "Utility which will duplicate and rename files in a directory",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec",
    "build": "babel src/index.js --out-file dist/bundle.js && uglifyjs dist/bundle.js -o dist/bundle.js"
  },
  "author": "Richard Xu <richardxu100@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "mocha": "^3.4.2",
    "vue-select": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "uglify-es": "^3.0.25"
  }
}
